# Dial2Note v2 – 3-Agent Pipeline
# Install: pip install -r requirements.txt

pandas>=1.5.0
numpy>=1.21.0
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.26.0

# NER (GLiNER – dialogue-only clinical entities)
gliner>=0.2.20

# Fine-tuning (Unsloth QLoRA)
unsloth>=2024.0
datasets>=2.14.0
trl>=0.7.0
peft>=0.7.0

# Batch inference
vllm>=0.4.0

# Evaluation
evaluate>=0.4.0
nltk>=3.8.0
